import ColumnMethodGame from "../ColumnMethodGame";

export default function ColumnSubtractionChallenge3({ onComplete }) {
  return <ColumnMethodGame operation="subtract" level={3} onComplete={onComplete} />;
}
