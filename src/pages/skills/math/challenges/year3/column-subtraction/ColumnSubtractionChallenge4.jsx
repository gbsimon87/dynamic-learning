import ColumnMethodGame from "../ColumnMethodGame";

export default function ColumnSubtractionChallenge4({ onComplete }) {
  return <ColumnMethodGame operation="subtract" level={4} onComplete={onComplete} />;
}
