import Year3FractionGame from "../Year3FractionGame";

function FractionsAsNumbersChallenge1({ onComplete }) {
  return <Year3FractionGame topic="numbers" level={1} onComplete={onComplete} />;
}

export default FractionsAsNumbersChallenge1;
