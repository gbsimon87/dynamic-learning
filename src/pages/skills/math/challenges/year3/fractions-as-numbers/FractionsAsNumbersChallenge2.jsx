import Year3FractionGame from "../Year3FractionGame";

function FractionsAsNumbersChallenge2({ onComplete }) {
  return <Year3FractionGame topic="numbers" level={2} onComplete={onComplete} />;
}

export default FractionsAsNumbersChallenge2;
