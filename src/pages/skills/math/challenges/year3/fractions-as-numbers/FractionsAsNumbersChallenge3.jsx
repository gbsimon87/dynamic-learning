import Year3FractionGame from "../Year3FractionGame";

function FractionsAsNumbersChallenge3({ onComplete }) {
  return <Year3FractionGame topic="numbers" level={3} onComplete={onComplete} />;
}

export default FractionsAsNumbersChallenge3;
