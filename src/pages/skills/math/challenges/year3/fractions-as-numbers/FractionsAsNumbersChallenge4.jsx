import Year3FractionGame from "../Year3FractionGame";

function FractionsAsNumbersChallenge4({ onComplete }) {
  return <Year3FractionGame topic="numbers" level={4} onComplete={onComplete} />;
}

export default FractionsAsNumbersChallenge4;
