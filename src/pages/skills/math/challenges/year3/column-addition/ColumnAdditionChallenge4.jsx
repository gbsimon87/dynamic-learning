import ColumnMethodGame from "../ColumnMethodGame";

export default function ColumnAdditionChallenge4({ onComplete }) {
  return <ColumnMethodGame operation="add" level={4} onComplete={onComplete} />;
}
