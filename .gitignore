# === Logs ===
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*
yarn-error.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# === Dependencies ===
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# === Build output ===
dist
dist-ssr
build
*.local

# === Vite / bundler caches ===
.vite
node_modules/.vite
node_modules/.cache
.rollup.cache
*.tsbuildinfo

# === Test & coverage output ===
# (No test runner yet — see docs/PROJECT_IDEAS.md #8. Ready for Vitest.)
coverage
*.lcov
.nyc_output

# === Linter caches ===
.eslintcache
.stylelintcache

# === Environment & secrets ===
# The app is fully client-side today, but anything in a Vite VITE_* var is
# bundled into the client. Never commit these.
.env
.env.*
!.env.example

# === Deployment platform artifacts ===
.vercel
.netlify
.firebase

# === Editor & IDE ===
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === OS cruft ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# === Claude Code ===
# .claude/settings.json and .claude/skills/ ARE committed — they are shared
# project configuration. Only personal, machine-local overrides are ignored.
.claude/settings.local.json
.claude/*.local.json
